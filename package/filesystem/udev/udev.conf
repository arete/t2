# --- T2-COPYRIGHT-NOTE-BEGIN ---
# This copyright note is auto-generated by ./scripts/Create-CopyPatch.
# 
# T2 SDE: package/.../udev/udev.conf
# Copyright (C) 2004 - 2011 The T2 SDE Project
# Copyright (C) 1998 - 2003 ROCK Linux Project
# 
# More information can be found in the files COPYING and README.
# 
# This program is free software; you can redistribute it and/or modify
# it under the terms of the GNU General Public License as published by
# the Free Software Foundation; version 2 of the License. A copy of the
# GNU General Public License can be found in the file COPYING.
# --- T2-COPYRIGHT-NOTE-END ---

# BK stuff removed.

if pkginstalled gobject-introspection; then
	var_append confopt " " " --enable-introspection"
else
 var_append confopt " " " --disable-introspection"
fi 

# BK
# --prefix=/usr --sysconfdir=/etc --localstatedir=/var --enable-introspection=no --build=i486-pc-linux-gnu
var_append confopt '' ' --sbindir=/sbin --libexecdir=/lib/udev --with-rootlibdir=/lib --with-firmware-path=/lib/firmware/updates:/lib/firmware --disable-selinux --enable-libkmod --with-rootprefix="" --disable-gtk-doc --disable-manpages --disable-keymap --with-modprobe=/sbin/modprobe --libexecdir=/usr/libexec --enable-modules --enable-split-usr --exec-prefix=""'

# BK remove:
#var_append confopt " " "--libexecdir=/lib/udev" # $libdir?

# BK stuff removed.

# BK eudev configure cannot find libkmod.pc...
udev_find_libkmod()
{
 if [! -f $root/usr/lib/pkgconfig/libkmod.pc];then cp -a $root/lib/pkgconfig/libkmod.pc $root/usr/lib/pkgconfig/; fi
}
hook_add preconf 5 "udev_find_libkmod"
